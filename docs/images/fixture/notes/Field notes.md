# Field notes

Two scans from the notebook, plus one photo straight off the phone. The `.heic` is
there on purpose: iOS shoots it by default and vision models reject it, so the sidebar
has to show it greyed out instead of failing halfway through a run.

![[field-notes.png]]

![[photo.heic]]
