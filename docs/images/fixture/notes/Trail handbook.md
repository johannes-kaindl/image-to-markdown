# Trail handbook

The printed handbook, three pages, born-digital with a real text layer — so the
"use embedded PDF text" path has something to find and the page-range picker has a
range worth picking.

![[trail-handbook.pdf]]
